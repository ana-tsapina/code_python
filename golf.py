total = int(input("Enter your total distance: "))
clubs = int(input("How many clubs do you have? "))
distance = []


i = 0 

for i in range(clubs): 
    value = (int(input(f"What is the distance at {i}? ")))
    distance.append(value)

    

# sort the club distance from max to min -> we want to use max 

